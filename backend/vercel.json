{
  "version": 2,
  "ignoreCommand": "git diff --quiet HEAD~1 HEAD -- ./backend",
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.js"
    }
  ]
}
